# Core dependencies for centralized exchanges (Binance, Coinbase, Kraken)
ccxt==4.4.1  # Pinned to avoid coincurve build dependency on Streamlit Cloud (4.4.0 doesn't exist)
pandas>=2.0.0
numpy>=1.24.0
streamlit>=1.28.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0

# DEX support (web3 works without coincurve)
web3>=6.0.0

# Visualization
plotly>=5.17.0

# Voice alerts (StarCraft-style notifications)
pyttsx3>=2.90
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.0.0  # Text-to-speech for voice alerts
openai>=1.0.0  # AI assistant integration (optional, paid)
groq>=0.4.0  # AI assistant integration (free tier available)
twitter-api-v2>=1.15.0  # X (Twitter) API v2 client

# Note: For full DEX support with ccxt 4.5+ (includes coincurve for performance),
# see requirements-dex.txt. coincurve requires pkg-config which may not be
# available on Streamlit Cloud. ccxt will fall back to pure Python ECDSA if
# coincurve is not available.

